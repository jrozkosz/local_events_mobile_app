# local_events_mobile_app

### run frontend

from \react_app:
npx expo start -c

### run backend

from \flask_app:
flask --app flaskr run --host=0.0.0.0 --port=5000 --debug

### access mysql from cmd

mysql -u root -p
